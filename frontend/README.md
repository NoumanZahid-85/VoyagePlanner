# VoyagePlanner Frontend

React + Vite + TypeScript SPA with Tailwind CSS, shadcn/ui, MapLibre GL JS, and TanStack Query.

## Getting Started
```bash
npm install
npm run dev
```

## Environment
Copy `.env` to set `VITE_MAPTILER_KEY` for map tiles and `VITE_API_URL` for the backend.
